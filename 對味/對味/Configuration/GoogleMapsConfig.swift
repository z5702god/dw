// Google Maps is no longer used — app uses Apple MapKit.
// API key removed for security. If Google Maps is needed in the future,
// store the key in Xcode build settings or a .xcconfig file, not in source code.
enum GoogleMapsConfig {
    // static let apiKey = "<load from environment or xcconfig>"
}
